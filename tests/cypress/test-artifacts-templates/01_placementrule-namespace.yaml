apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: towhichcluster-$JOB_ID
  namespace: ns-sub-ns-$JOB_ID
  labels:
    name: ns-sub-$JOB_ID
spec:
  clusterReplicas: 1
  clusterLabels:
    matchLabels:
      environment: Dev