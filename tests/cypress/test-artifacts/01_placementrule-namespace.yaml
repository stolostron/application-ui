apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: towhichcluster
  namespace: ns-sub-ns
  labels:
    name: ns-sub
spec:
  clusterReplicas: 1
  clusterLabels:
    matchLabels:
      environment: Dev