{{! ========================================================== }}
{{! ========================================================== }}
{{! ================== Ansible Secret ======================== }}
{{! ========================================================== }}
{{! ========================================================== }}
{{#if ansibleTowerHost}}
---
apiVersion: v1
kind: Secret
metadata:
  labels:
    app: {{{@root.name}}}
  name: {{{ansibleSecretName}}}
  namespace: {{{@root.namespace}}}
data:
  host: {{{ansibleTowerHost}}}
  token: {{{ansibleTowerToken}}}
 {{/if}} 
