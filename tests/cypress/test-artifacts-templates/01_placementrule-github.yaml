apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: towhichcluster-$JOB_ID
  namespace: git-sub-ns-$JOB_ID
  labels:
    name: git-sub-$JOB_ID
spec:
  clusterReplicas: 1
  clusterLabels:
    matchLabels:
      environment: env