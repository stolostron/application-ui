apiVersion: v1
kind: Namespace
metadata:
  name: obj-sub-ns-$JOB_ID
---
apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  name: obj-sub-$JOB_ID
  namespace: obj-sub-ns-$JOB_ID
  labels:
    name: obj-sub-$JOB_ID
spec:
  channel: ch-obj-$JOB_ID/deva
  placement:
    placementRef: 
      name: towhichcluster-$JOB_ID
      kind: PlacementRule   