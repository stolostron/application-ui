apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: towhichcluster-$JOB_ID
  namespace: obj-sub-ns-$JOB_ID
  labels:
    name: obj-sub-$JOB_ID
spec:
  clusterReplicas: 1
  clusterLabels:
    matchLabels:
      environment: Dev