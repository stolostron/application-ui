apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: obj-sub-app-$JOB_ID
  namespace: obj-sub-ns-$JOB_ID
spec:
  componentKinds:
    - group: apps.open-cluster-management.io
      kind: Subscription
  descriptor: {}
  selector:
    matchLabels:
      name: obj-sub-$JOB_ID