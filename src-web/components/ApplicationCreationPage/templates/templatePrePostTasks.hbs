{{! ========================================================== }}
{{! ========================================================== }}
{{! ================== Ansible Secret ======================== }}
{{! ========================================================== }}
{{! ========================================================== }}
{{#if ansibleSecretName}}
---
apiVersion: v1
kind: Secret
metadata:
  labels:
    app: {{{@root.name}}}
  name: {{{ansibleSecretName}}}
  namespace: {{{@root.namespace}}}
{{#if @root.showSecrets}}
data: {{{@root.metadata}}}
{{else}}
data: # injected on create
{{/if}}
{{/if}} 
