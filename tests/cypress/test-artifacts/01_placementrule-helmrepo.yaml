apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: towhichcluster
  namespace: helm-sub-ns
  labels:
    name: helm-sub
spec:
  clusterReplicas: 1
  clusterLabels:
    matchLabels:
      environment: Dev