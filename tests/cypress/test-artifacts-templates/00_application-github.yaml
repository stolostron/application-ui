apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: git-sub-app-$JOB_ID
  namespace: git-sub-ns-$JOB_ID
spec:
  componentKinds:
    - group: apps.open-cluster-management.io
      kind: Subscription
  descriptor: {}
  selector:
    matchLabels:
      name: git-sub-$JOB_ID