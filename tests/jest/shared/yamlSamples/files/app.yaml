apiVersion: v1                      # Optional. Remove the fields for creating the application namespace resource when the required namespace resource already exists.
kind: Namespace
metadata:
  name:                             # Required. The name of the application namespace resource to create.
---
apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name:                           # Required. The name of the application resource to create.
  namespace:                      # Required. The namespace resource to use for the application.
spec:
  componentKinds:
  - group: apps.open-cluster-management.io
    kind: Subscription
  descriptor: {}
  selector:
    matchExpressions:
      - key:                      # Required. The Kubernetes label key that a resource needs to match.
        operator: In
        values:                   # Required. The Kubernetes label values that a resource needs to match.