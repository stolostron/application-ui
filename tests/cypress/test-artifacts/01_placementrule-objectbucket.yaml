apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: towhichcluster
  namespace: obj-sub-ns
  labels:
    name: obj-sub
spec:
  clusterReplicas: 1
  clusterLabels:
    matchLabels:
      environment: Dev